

{% extends 'index.html.twig' %}

{% block body %}
 <style>
        h1 {
            text-align: center; /* Centrer le titre */
            color: #ff0000; /* Rouge */
            font-size: 24px;
            margin-bottom: 20px;
            font-family: 'Arial', sans-serif;
        }
    </style>
    <table class="table">
        <tbody>
            <tr>
                <th>Numord</th>
                <td>{{ esp_contrat.numord }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ esp_contrat.annee }}</td>
            </tr>
            <tr>
                <th>Date_etab</th>
                <td>{{ esp_contrat.dateEtab ? esp_contrat.dateEtab|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Diplome</th>
                <td>{{ esp_contrat.diplome }}</td>
            </tr>
            <tr>
                <th>Grade</th>
                <td>{{ esp_contrat.grade }}</td>
            </tr>
            <tr>
                <th>Institution</th>
                <td>{{ esp_contrat.institution }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_esp_contrat_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_esp_contrat_edit', {'numord': esp_contrat.numord}) }}">Modifier </a>

    {{ include('esp_contrat/_delete_form.html.twig') }}
{% endblock %}
