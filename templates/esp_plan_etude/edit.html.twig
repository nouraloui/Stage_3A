{% extends 'index.html.twig' %}

{% block body %}
 <style>
        h1 {
            text-align: center; /* Centrer le titre */
            color: #ff0000; /* Rouge */
            font-size: 24px;
            margin-bottom: 20px;
            font-family: 'Arial', sans-serif;
        }
    </style>
    <h1>Modifier EspPlanEtude</h1>

    {{ include('esp_plan_etude/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('planEtudeIndex') }}">Retourner à la liste</a>

    {{ include('esp_plan_etude/_delete_form.html.twig') }}
{% endblock %}
