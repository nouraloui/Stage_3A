<form method="post" action="{{ path('app_esp_saison_universitaire_delete', {'id': esp_saison_universitaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ esp_saison_universitaire.id) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
