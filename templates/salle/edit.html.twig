{% extends 'index.html.twig' %}

{% block body %}
 <style>
        h1 {
            text-align: center; /* Centrer le titre */
            color: #ff0000; /* Rouge */
            font-size: 24px;
            margin-bottom: 20px;
            font-family: 'Arial', sans-serif;
        }
    </style>
    <h1>Modifier Salle</h1>

    {{ include('salle/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_salle_index') }}">Retourner à la liste</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
